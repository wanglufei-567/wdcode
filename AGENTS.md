## **AI Coding** 执行约束

### 一、编码前阅读顺序

1. `README.md`
2. `docs/SPEC.md`
3. `docs/ARCHITECTURE.md`
4. `docs/CODING_STANDARD.md`
5. 目标目录自己的 `docs/SPEC.md`
6. 涉及生产运行时继续阅读 `docs/DEPLOYMENT.md`

### 二、责任边界

- `apps/personal-website` 只拥有个人站应用和对外部内容的展示投影
- `infra` 只拥有生产入口、路由、镜像选择、运行编排和运维策略
- 外部项目的业务源码、构建规则和镜像契约由各自仓库拥有
- 禁止通过相对路径读取兄弟仓库源码作为生产构建上下文
- 禁止把服务器密钥、证书私钥、生产环境变量或业务持久化数据提交到 **Git**

### 三、变更纪律

- 应用变更不得隐式触发基础设施全量发布
- 基础设施变更必须说明影响的域名、服务、端口、数据卷和回滚路径
- 文档必须区分当前实现、目标架构和未来选项
- 新增长期规则时更新对应真值文档，不在多个文件复制完整规则
- 未经明确要求不提交、不推送、不执行生产部署
